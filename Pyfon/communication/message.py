class Message():
    def __init__(self, robotId, message):
    """Message Constructor
        
        Store message and robot id
        
        Args:
            robotId (int): Robot id
            message (string): message to send

        Returns:

    """
        self.robotId = robotId
        self.message = message
